
### Serving UMD and running the app

Locate where the umd build is, e.g) ObjectModel/src/Wasm/Umd/bin

From the above path run `npx serve -l 4444` to serve the umd build.

Run `npm start` or `yarn start` to start up the app which should boot the wasm
when application starts.
